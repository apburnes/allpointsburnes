---
interface Props {
  title: string;
  link?: string;
  linkText?: string;
}

const { title, link, linkText } = Astro.props;
---

<article class="prose max-w-none grid w-full place-items-center py-12 text-center">
  <h2>
    {title}
  </h2>
  {
    link && linkText ? (
      <a href={link} class="link muted-link text-sm">
        {linkText}
      </a>
    ) : null
  }
</article>
